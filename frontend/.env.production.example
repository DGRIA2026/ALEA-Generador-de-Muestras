# Fallback integrado al build para Electron o despliegues estaticos sin el
# runtime-config.js generado por el contenedor. No configura Nginx en runtime.
VITE_API_URL=https://alea.sesna.gob.mx/RUTA_BACKEND
